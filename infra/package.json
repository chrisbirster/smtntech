{
  "name": "south-mountain-tech-infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@10.9.2",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "sst install && tsc --noEmit",
    "deploy": "sst deploy",
    "deploy:production": "sst deploy --stage production",
    "remove": "sst remove",
    "dev": "sst dev"
  },
  "dependencies": {
    "@pulumi/pulumi": "^3.0.0",
    "sst": "^3.18.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.9.2"
  }
}
